<!DOCTYPE html>
<html>
<body>
  <div class="hero-body is-fullheight container-sign">
    <div class="container has-text-centered">
      <%= link_to(image_tag("ComponentNW.png", :class => 'nw'), "/")%>
    </div>
    <div class="container has-text-centered lema-position">
      <div class="login-lema">
        <span>learn.</span>
        <span class="talk-color">talk.</span>
        <span class="initiate-color">initiate.</span>
      </div>
    </div>
    <br>
    <div class="container has-text-centered options-sign">
      <a class="btn-login" href="/users/sign_in">Entrar</a>
      <a class="btn-login" href="/users/sign_up">Registar</a>
    </div>
    <br>
    <br>
    <div class="column is-4 has-text-centered box-white">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'email-field',placeholder:'Email' %>
        </div>

        <div class="field">
          <%= f.password_field :password, autocomplete: "off", class: 'email-field',placeholder:'Password' %>
        </div>
<br>
        <div class="field remember">
        <% if devise_mapping.rememberable? -%>
            <%= f.check_box :remember_me, class: 'checkbox-remember' %>
            <%= f.label "Lembrar-me", class: 'remember-text' %>
            <% end -%>
          </div>
          <br>
          <div class="field remember">
            <a class="remember-text" href="/users/password/new">Esqueci-me password</a>
          </div>

        <br>

        <div class="actions">
          <%= f.submit "Entrar", class: 'btn-login' %>
        </div>

      <% end %>
    </div>

  </div>
</body>
</html>

